// Rules for naming Variable

//  -   name can be alphanumeric (it's first word MUST be aplhabet or underscore(_) ONLY, not a numrical) 
//  -   no commas, blanks allowed
//  -   no special symbols or keywords allowed
//  -   varibles are CASE SENSITIVE

#include<stdio.h>
int main()  // execution always start from main()
{
    int t45om;
    return 0;
}

// Characters must enclosed within Single Inverted Commas

// 32 reserved keywords in C

// compiler convers a c program into machine lagangue(binary file)

// (&)  -   Address of operator, i.e., supplied value is copied to address of variable